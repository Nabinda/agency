//- Schedule Row Mixin
//- Used in: Employee Dashboard
//- Params:
//-   startDate: Date string (e.g. "Monday, Jan 15")
//-   endDate: Time/Duration string (e.g. "9:00 AM - 5:00 PM • 8h")
//-   status: Enum string ('completed', 'working', 'scheduled')
mixin scheduleRow(startDate, endDate, status)
  .list-group-item.bg-transparent.schedule-item.px-4
    .row.align-items-center
      .col-12.col-md-6.mb-2.mb-md-0
        h6.fw-bold.mb-1= startDate
        span.text-muted.small= endDate
      .col-12.col-md-6.text-md-end
        
        //- Switch logic based on the status string
        case status
          when 'completed'
            //- Green Badge
            .status-badge
              +icon('check', 'me-1')
              | Completed
          
          when 'working'
            //- Blue Badge (Active)
            .status-badge(style="background-color: rgba(59, 130, 246, 0.15); color: #3b82f6;")
              +icon('hourglass', 'me-1')
              | Working Now
          
          default
            //- Yellow Badge (Pending)
            .status-badge(style="background-color: rgba(234, 179, 8, 0.15); color: #eab308;")
              +icon('calendar', 'me-1')
              | Scheduled



//- holidayRequestRow(date, days, reason, status, statusColorVar)
//- Renders a single row in the Holiday Request History list.
//- statusColorVar should be a CSS variable like 'var(--accent-green)'
mixin holidayRequestRow(date, days, reason, status, statusColorVar)
  .list-group-item.schedule-item.p-4(style="background-color: transparent;")
    .d-flex.align-items-start.justify-content-between.flex-wrap
      .me-4.mb-2.mb-sm-0
        h6.fw-bold.mb-1= date
        p.text-muted.small.mb-0= reason
      .d-flex.align-items-center
        .text-end.me-3
          h6.fw-bold.mb-1.text-primary= days
          p.text-muted.small.mb-0 Total Days
        span.status-badge(class=`status-badge-${status.toLowerCase()}`)
          | #{status}



//- Request History Row Mixin
//- Used in: Employee Holiday/Advance Pages
//- Params:
//-   title: Main text (e.g. Date range or Date)
//-   subtitle: Secondary text (e.g. Duration or Amount)
//-   description: Description text
//-   status: Status string (Approved, Rejected, Pending)
//-   statusColor: Color variable/hex for status badge
mixin requestHistoryRow(title, subtitle, description, status, statusColor)
  .list-group-item.bg-transparent.p-4.border-bottom.border-light.border-opacity-10
    .d-flex.justify-content-between.align-items-center
      div
        h6.fw-bold.mb-1= title
        span.badge.bg-secondary.bg-opacity-25.text-white.me-2= subtitle
        small.text-muted= description
      div
        span.badge(style=`background-color: ${statusColor}25; color: ${statusColor}; border: 1px solid ${statusColor}50`)
          = status